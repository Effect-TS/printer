/**
 * A document containing a single `\` character.
 *
 * @tsplus static ets/printer/Doc/Ops backslash
 */
export const backslash: Doc<never> = Doc.char("\\");

/**
 * A document containing a single `:` character.
 *
 * @tsplus static ets/printer/Doc/Ops colon
 */
export const colon: Doc<never> = Doc.char(":");

/**
 * A document containing a single `,` character.
 *
 * @tsplus static ets/printer/Doc/Ops comma
 */
export const comma: Doc<never> = Doc.char(",");

/**
 * A document containing a single `.` character.
 *
 * @tsplus static ets/printer/Doc/Ops dot
 */
export const dot: Doc<never> = Doc.char(".");

/**
 * A document containing a single `"` character.
 *
 * @tsplus static ets/printer/Doc/Ops dquote
 */
export const dquote: Doc<never> = Doc.char("\"");

/**
 * A document containing a single `=` character.
 *
 * @tsplus static ets/printer/Doc/Ops equalSign
 */
export const equalSign: Doc<never> = Doc.char("=");

/**
 * A document containing a single `<` character.
 *
 * @tsplus static ets/printer/Doc/Ops langle
 */
export const langle: Doc<never> = Doc.char("<");

/**
 * A document containing a single `{` character.
 *
 * @tsplus static ets/printer/Doc/Ops lbrace
 */
export const lbrace: Doc<never> = Doc.char("{");

/**
 * A document containing a single `[` character.
 *
 * @tsplus static ets/printer/Doc/Ops lbracket
 */
export const lbracket: Doc<never> = Doc.char("[");

/**
 * A document containing a single `(` character.
 *
 * @tsplus static ets/printer/Doc/Ops lparen
 */
export const lparen: Doc<never> = Doc.char("(");

/**
 * A document containing a single `>` character.
 *
 * @tsplus static ets/printer/Doc/Ops rangle
 */
export const rangle: Doc<never> = Doc.char(">");

/**
 * A document containing a single `}` character.
 *
 * @tsplus static ets/printer/Doc/Ops rbrace
 */
export const rbrace: Doc<never> = Doc.char("}");

/**
 * A document containing a single `]` character.
 *
 * @tsplus static ets/printer/Doc/Ops rbracket
 */
export const rbracket: Doc<never> = Doc.char("]");

/**
 * A document containing a single `)` character.
 *
 * @tsplus static ets/printer/Doc/Ops rparen
 */
export const rparen: Doc<never> = Doc.char(")");

/**
 * A document containing a single `;` character.
 *
 * @tsplus static ets/printer/Doc/Ops semi
 */
export const semi: Doc<never> = Doc.char(";");

/**
 * A document containing a single `/` character.
 *
 * @tsplus static ets/printer/Doc/Ops slash
 */
export const slash: Doc<never> = Doc.char("/");

/**
 * A document containing a single `'` character.
 *
 * @tsplus static ets/printer/Doc/Ops squote
 */
export const squote: Doc<never> = Doc.char("'");

/**
 * A document containing a single ` ` character.
 *
 * @tsplus static ets/printer/Doc/Ops space
 */
export const space: Doc<never> = Doc.char(" ");

/**
 * A document containing a single `|` character.
 *
 * @tsplus static ets/printer/Doc/Ops vbar
 */
export const vbar: Doc<never> = Doc.char("|");
